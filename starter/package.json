{
  "name": "portfolio-homepage",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sass:build": "sass src/main.scss dist/main.css --style=compressed",
    "sass:watch": "sass --watch src/main.scss:dist/main.css --style=expanded",
    "start": "npm run sass:watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "less-watch-compile": "^2.1.0",
    "sass": "^1.79.3"
  },
  "devDependencies": {
    "less": "^4.2.0"
  }
}


